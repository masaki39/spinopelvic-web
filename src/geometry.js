//==================================================================
// 幾何ユーティリティ（純粋関数）
//==================================================================
export const G = {
  midpoint:(a,b)=>({x:(a.x+b.x)/2, y:(a.y+b.y)/2}),
  distance:(a,b)=>Math.hypot(a.x-b.x, a.y-b.y),
};
